# -*- coding: utf-8 -*-
# 시장 반응 시뮬레이션 (market_reaction) — Python 의존성
# 현재 서비스는 DB를 직접 사용하지 않는 stateless 분석 API.
# 저장은 후속 Node backend + MongoDB 단계에서 처리한다.

# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Data validation / settings
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1

# HTTP client (Ollama 비동기 호출용)
httpx[socks]==0.28.1

# RAG (문서 임베딩/벡터 검색)
faiss-cpu==1.9.0
numpy==1.26.4
beautifulsoup4==4.12.3

# MongoDB (RAG 저장소 — 빌드 스크립트 + 런타임 둘 다 사용, Motor 는 deprecated/EOL 이라 안 씀)
# dnspython 은 pymongo 4.17 기준 별도 extra 없이 기본 의존성으로 포함되어 mongodb+srv:// 를 바로 지원한다
pymongo==4.17.0

# Testing
pytest==8.3.4
pytest-asyncio==0.25.0
